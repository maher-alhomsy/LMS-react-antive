import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const CourseAccessScreen = () => {
  return (
    <View>
      <Text>CourseAccessScreen</Text>
    </View>
  );
};

export default CourseAccessScreen;

const styles = StyleSheet.create({});
